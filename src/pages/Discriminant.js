import 'katex/dist/katex.min.css';
import ReactMarkdown from 'react-markdown';
import remarkMath from 'remark-math';
import rehypeKatex from 'rehype-katex';
import rehypeRaw from 'rehype-raw'; // Поддержка HTML

function Home() {
  const mathContent = `
# Что такое квадратное уравнение?

Квадратное уравнение — это уравнение вида:

$$
ax^2 + bx + c = 0 \\quad (a \\neq 0)
$$

Оно называется "квадратным", потому что переменная $x$ стоит во второй степени. Чтобы найти корни такого уравнения, нам понадобится **дискриминант**.

## 🔍 Что такое дискриминант?

Дискриминант — это выражение, которое помогает определить, сколько корней имеет уравнение. Он вычисляется по формуле:

$$
D = b^2 - 4ac
$$

А теперь давай посмотрим, как его получить!

## Вывод формулы дискриминанта

1. Начинаем с исходного уравнения:

$$
ax^2 + bx + c = 0
$$

2. Делим всё на $a$, чтобы сделать коэффициент при $x^2$ равным 1:

$$
x^2 + \\frac{b}{a}x + \\frac{c}{a} = 0
$$

3. Теперь выделим полный квадрат:

$$
x^2 + 2 \\cdot \\frac{b}{2a}x + \\left(\\frac{b}{2a}\\right)^2 - \\left(\\frac{b}{2a}\\right)^2 + \\frac{c}{a} = 0
$$

4. Это можно переписать так:

$$
\\left(x + \\frac{b}{2a}\\right)^2 - \\frac{b^2}{4a^2} + \\frac{c}{a} = 0
$$

5. Переносим лишнее вправо:

$$
\\left(x + \\frac{b}{2a}\\right)^2 = \\frac{b^2}{4a^2} - \\frac{c}{a}
$$

6. Приведём к общему знаменателю:

$$
\\left(x + \\frac{b}{2a}\\right)^2 = \\frac{b^2 - 4ac}{4a^2}
$$

7. Извлекаем корень:

$$
x + \\frac{b}{2a} = \\pm \\frac{\\sqrt{b^2 - 4ac}}{2a}
$$

8. Переносим слагаемое:

$$
x = \\frac{-b \\pm \\sqrt{b^2 - 4ac}}{2a}
$$

Именно поэтому **дискриминант** — это то, что под корнем:

$$
D = b^2 - 4ac
$$

## Анализ дискриминанта

- Если $D > 0$ — уравнение имеет **два различных действительных корня**.
- Если $D = 0$ — есть **один корень (кратный двум)**.
- Если $D < 0$ — **корней нет среди действительных чисел**, но есть **комплексные корни**.

## Зачем это нужно?

Понимание дискриминанта помогает решать задачи в физике, инженерии, экономике и даже в играх! Например, чтобы рассчитать траекторию мяча или найти момент максимальной прибыли в бизнесе.

## Задачки для тренировки

1. Решите уравнение: $x^2 - 5x + 6 = 0$. Найдите дискриминант и корни.
2. Сколько корней имеет уравнение $x^2 - 4x + 4 = 0$?
3. А у этого уравнения? $x^2 + x + 1 = 0$

Попробуй решить их самостоятельно, а потом проверь!
`;

  return (
    <div className="container markdown">
      <ReactMarkdown
        children={mathContent}
        remarkPlugins={[remarkMath]}
        rehypePlugins={[rehypeKatex, rehypeRaw]}
      />
    </div>
  );
}

export default Home;